{
  "name": "oceaniaradio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "db:init": "wrangler d1 execute oceaniaradio-db --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250506.0",
    "wrangler": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
